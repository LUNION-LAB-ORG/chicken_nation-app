/**
 * Constantes pour les endpoints de l'API liés aux menus
 */
export const API_ENDPOINTS = {
  // Endpoints corrects sans le préfixe /api/ qui est déjà dans la baseURL
  DISHES: "/v1/dishes",
  MENU_CATEGORIES: "/v1/menu-categories",
  SUPPLEMENTS: "/v1/supplements",
};
